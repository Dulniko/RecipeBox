{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold mb-6">Edit Tag</h1>

    <div class="bg-white shadow-md rounded-lg p-6 mb-6">
        {{ include('tag/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_tag_index') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">Back to list</a>
    </div>

    <div class="mt-4">
        {{ include('tag/_delete_form.html.twig') }}
    </div>
{% endblock %}
