{% extends 'base.html.twig' %}

{% block title %}New Recipe{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold mb-6">Create new Recipe</h1>

    <div class="bg-white shadow-md rounded-lg p-6 mb-6">
        {{ include('recipe/_form.html.twig') }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_recipe_index') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">Back to list</a>
    </div>
{% endblock %}
