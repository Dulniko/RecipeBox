<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body class="bg-gray-100 text-gray-900">
        <header class="bg-white shadow">
            <div class="container mx-auto px-4 py-6 flex justify-between items-center">
                <h1 class="text-3xl font-bold">
                    <a href="{{ path('home') }}" class="text-gray-900 hover:text-gray-600">RecipeBox</a>
                </h1>
                <nav>
                {% if app.user %}
                    <span class="text-gray-700 mr-4">Logged in as {{ app.user.email }}</span>
                    <a href="{{ path('app_logout') }}" class="bg-gray-700 hover:bg-gray-900 text-white font-bold py-2 px-4 rounded">Logout</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded mr-2">Login</a>
                    <a href="{{ path('app_register') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">Register</a>
                {% endif %}
                </nav>
            </div>
        </header>
        <main class="container mx-auto px-4 py-6">
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
